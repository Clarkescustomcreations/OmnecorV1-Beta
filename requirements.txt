# Omnecor HMCI — Python Bridge Dependencies
# Install with: pip install -r requirements.txt
#
# Core stack (required by all bridges):
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0
requests>=2.31.0

# Audio / Voice bridges (tts_server.py, whisper_server.py, rvc_server.py):
# torch is a heavy install; install the CPU or CUDA variant from https://pytorch.org/
torch>=2.2.0
# coqui-tts is the maintained fork of the (now-abandoned) Coqui "TTS" package.
# It is a drop-in replacement — same `from TTS.api import TTS` import — and,
# unlike the original (capped at 0.22.0, Requires-Python <3.12), it ships
# wheels for Python 3.12+. The old `TTS>=0.22.0` was unresolvable on modern runners.
coqui-tts>=0.25.0
faster-whisper>=1.0.0
numpy>=1.26.0
soundfile>=0.12.1

# Fine-tuning bridge (localLLMfine-tuning.py):
transformers>=4.57,<5  # coqui-tts 0.27.x uses isin_mps_friendly (removed in 5.x)
trl>=0.8.0
datasets>=2.19.0

# Fal.ai bridge (fal_bridge.py):
fal-client>=0.5.0

# --- Optional dependencies (install only if you need the feature) ---

# Llama.cpp direct inference (llamacpp_bridge.py):
# pip install llama-cpp-python
# For CUDA: CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python

# YARA malware rule scanning (threat_scanner.py):
# pip install yara-python

# Semgrep static analysis (threat_scanner.py):
# pip install semgrep

# Unsloth accelerated fine-tuning (localLLMfine-tuning.py, GPU only):
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
python-multipart>=0.0.9
torchaudio>=2.2.0
torchcodec  # coqui-tts audio IO on torch>=2.9
